"""Regression tests for the HAM worked-example library.

Each test module imports the corresponding `examples/<name>.py` factories
and pins the HAM output against a known analytic reference (closed-form
Taylor, residual-norm bound, optimal-ℏ grid selection, validity-gate).
"""
