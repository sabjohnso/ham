"""HAM worked-example library.

Each submodule is a self-contained example: an importable factory
(`build_problem`, `solve_to`, `analyze`, `is_convergent`) plus an
`if __name__ == "__main__":` block that runs the diagnostics and prints
results. Regression tests live under `tests/examples/`.
"""
